import (
	body.SimpleConsole,
) -> ConsoleProgram :: (c, _, _) -> (print (c,"Hello World")), 0);

"""
Should generate in c

#include <stdio.h>
int main() {
   printf("Hello, World!");
   return 0;
}
"""