List: 
    given list_type@Any
    ->  return |
            empty: `empty_list,
            nonempty: (
                head: $list_type,
                tail: $List[$list_type]
            )
        |