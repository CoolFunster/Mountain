BalanceSheet:
    import $Float
    -> define PositiveFloat: {d@Float | d >= 0.0}
    -> return {
        bs@(	
            assets:$PositiveFloat,
            liabilities:$PositiveFloat,
            equity:$PositiveFloat
        )
        | $bs.assets = $bs.liabilities + $bs.equity
    }